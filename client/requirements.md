## Packages
framer-motion | Essential for soft, friendly page transitions and micro-interactions
recharts | For visualizing job fit scores and skill gaps with beautiful charts
date-fns | For human-readable dates in the application tracker
clsx | For conditional class names (standard utility)
tailwind-merge | For merging tailwind classes intelligently

## Notes
- Tailwind Config: Font families extended to include 'Outfit' (display) and 'Plus Jakarta Sans' (body).
- API: Assumes backend routes are ready at /api/*.
- Auth: Prototype mode - uses localStorage to persist a simple user session ID if needed, or defaults to userId=1.
- Images: Using Unsplash for friendly, contextual visuals in the Welcome/Onboarding flow.
